<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <!-- Headless harness. CI measures parse, serialise and validation timings through this
       without needing a display. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <AssemblyName>xsdedit</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../XsdEditor.Core/XsdEditor.Core.csproj" />
  </ItemGroup>

</Project>
