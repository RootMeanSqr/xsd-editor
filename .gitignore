# OS noise
.DS_Store
Thumbs.db

# Editor and IDE
.vscode/
.idea/
*.swp
*~

# Environment and secrets
.env
.env.*
!.env.example
.envrc

# Logs and temporary output
*.log
tmp/

# .NET
bin/
obj/
[Bb]uild[Ll]og.*
*.user
*.suo
artifacts/
publish/
TestResults/
BenchmarkDotNet.Artifacts/
.vs/

# Local corpus fixtures. The reference corpus is never committed; XSDEDITOR_CORPUS
# points at it instead (docs/decisions/0004-build-and-security-tooling.md).
corpus/

# The composition-scan report. scripts/check-vulnerable-packages.sh writes it into the
# repository root, so running the check locally the way CI does would otherwise leave an
# untracked file behind for the next person to wonder about.
vulnerable.txt
