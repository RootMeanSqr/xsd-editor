# CLAUDE.md

The working agreements for this repository live in [`AGENTS.md`](AGENTS.md).
Read that file first; it is the single source of truth for conventions, layout,
and build commands, and applies to Claude Code exactly as written.

Keep guidance in `AGENTS.md` rather than duplicating it here, so that every
agent and every human reads the same instructions.
